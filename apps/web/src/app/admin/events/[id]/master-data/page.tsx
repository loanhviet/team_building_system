"use client";

// Keep master-data work in the active event workspace for uninterrupted
// navigation; the underlying data remains shared across all events.
export { default } from "@/app/admin/master-data/page";
