"use client";

// Company data is deliberately rendered inside the selected event workspace:
// organizers can review a person or a team without losing the event context.
export { default } from "@/app/admin/employees/page";
